# 작업 규칙

  - 작업 폴더는 C:\Users\click\upstage_mabc다. 다른 폴더는 건드리지 않는다.
  - 현재 제품은 이주노동자 일·월급 웹앱이다. 예전 복지 README/BRAINSTORM은 과거 자료다.
  - 작업 시작 시 이 파일과 docs/ops/PROGRESS.md를 읽고, 이번 요청 범위부터 확인한다.
  - 기존 파일은 읽고 수정한다. 요청한 경로만 바꾸고, 삭제·이동은 별도 허락을 받는다.
  - 저장 후 같은 파일을 다시 읽고 git status --short --untracked-files=all로 변경 범위를 확인한다.
  - 생성 성공과 재열람을 확인한 뒤에만 완료를 기록한다. 생성과 완료 기록을 동시에 실행하지 않는다.
  - 실행·검증하지 않은 것은 미실행/미확인, 실패한 것은 실패로 적는다. 완료라고 추측하지 않는다.
  - 요청 범위 안에서 발견한 오류는 수정하고 재검증한다. 다음 작업은 임의로 시작하지 않는다.
  - 진행 상태·실제 변경 파일·남은 확인·다음 작업은 docs/ops/PROGRESS.md에 남긴다.
  - 비밀 파일이나 키 값은 읽거나 출력하지 않는다. 승인 차단을 다른 도구나 경로로 우회하지 않는다