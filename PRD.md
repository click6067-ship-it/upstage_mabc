# PRD.md

## 1. 제품 개요

이주노동자 일·월급 웹앱. 모바일·데스크톱 양쪽 대응.
월급에 의문이 생긴 이주노동자 본인이 들어와서 확인하고, 문의·상담 자료를 준비하고, 답변·보완 요청·수정 명세서 연결을 거쳐 쟁점별로 정리하고, 답변·추가 지급을 정리하고, 다음 급여기간 기록까지 이어가는 흐름을 지원한다.
한 일자리 기준으로 급여기간별 기록을 남긴다.
시작은 4개 버튼: `월급 못 받음`, `적거나 달라짐`, `명세서 이해`, `미리 기록`.
챗봇은 보조 수단이며, 단독 판단 주체가 아니다.

## 2. 사용자

- 이주노동자 본인: 한 일자리의 급여기간별 기록을 확인하고, 임금·명세서·출퇴근·휴게·증빙을 기록/확인하고, 문의 문구를 준비한다.
- 상담자: 사용자가 제공한 자료를 받는 쪽. 대리 입력·대리 관리 기능은 추가하지 않는다. 사용자가 넘긴 자료를 바탕으로 문의·상담 자료를 받는 역할로 본다.

언어 필요: 한국어·영어·베트남어·네팔어·크메르어. UI/안내/출력 문구를 이에 맞춰 제공한다.

## 3. 문제 정의

- 월급이 예상보다 적거나, 예상보다 많거나, 아예 못 받았거나, 항목이 왜 이렇게 나뉘는지 이해가 안 된다.
- 명세서와 실제 지급·기록 사이에 차이가 느껴질 때가 있다.
- 이전 급여기간과 이번 급여기간을 비교해야 감이 오는데, 그 비교가 쉽지 않다.
- 자료가 없거나 불완전해도 확인 질문과 자료 요청으로 시작할 수 있어야 한다. 사진·PDF가 있으면 활용하되, 원문 대조와 사용자 확인을 거친다.

## 4. 입출력

### 4.1 입력

- 4개 시작 버튼 선택에 따른 문답/수동 입력
- 버튼·언어를 바꿔도 같은 급여기간 기록의 입력 상태는 유지된다.
- 월급 관련 입력: 지급 내역, 항목, 금액, 날짜, 지급 조건, 메모
- 지급은 해당 급여기간에 연결한 것만 다룬다. 분할 지급·현금 지급·중복 지급 가능성을 확인한다.
- 출퇴근·휴게·메모·증빙: 날짜별 기록, 상태(예정/실제/회상/미기록), 증빙(사진·PDF 등)
- 언어 선택: 한국어·영어·베트남어·네팔어·크메르어
- 문서: 사진, PDF. OCR로 읽되 원문 대조와 사용자 확인이 필요하다. 실패하거나 모호한 부분은 수동으로 이어서 입력한다.
- 참고 수정본: 같은 기간의 이전 버전 명세서나 수정본. doc-compare로 항목 차이를 비교할 수 있다.

### 4.2 출력

- 요약 보기: 현재 확인 중인 월급 문제를 한 화면으로 정리
- 대조 결과: 명세서와 해당 지급 대조, 전월·당월 비교
- 문의·상담 선택 화면: 회사 문의/상담 선택 → 답변·보완 요청·수정 명세서 연결 → 사용자 확인 후 쟁점별 정리
- 문의·상담 준비 자료: 한국어와 선택 언어를 같은 사실·같은 버전으로 함께 확인·출력하는 문구·정리본
- 저장·내보내기: 내 기기 저장, 임시 이용, 백업/복원, 삭제
- 진행 중 남은 의문 목록: 지급 약속, 실제 지급, 아직 남은 의문을 따로 표시

## 5. 흐름 (시작 → 끝까지)

1. 시작
   - 4개 버튼 중 하나 선택: `월급 못 받음` / `적거나 달라짐` / `명세서 이해` / `미리 기록`
   - 자료가 없어도 확인 질문과 자료 요청으로 시작할 수 있다. 입력 내용은 저장되고, 자료가 생기면 같은 급여기간 기록에서 이어간다.
   - 버튼·언어를 바꿔도 이미 입력한 내용은 유지된다.

2. 확인
   - 사용자가 가진 자료(명세서, 지급 내역, 기록, 사진·PDF)를 확인
   - OCR은 원문 대조와 사용자 확인을 거쳐 활용한다. 실패·불확실하면 수동으로 넘어간다.
   - 모르는 값은 0으로 채우지 않는다. 추정·누락은 별도 상태로 표시한다.
   - 자료는 해당 급여기간에 연결한다. 지급은 해당 급여기간에 연결한 것만 다루고, 분할·현금·중복 가능성을 확인한다.

3. 대조·비교
   - 명세서와 해당 지급을 대조한다.
   - 전월·당월을 비교한다.
   - 입력 조건에 따른 기본급 산술은 별도로 구분한다. 계산 결과를 바로 체불 확정으로 바꾸지 않는다.
   - 차액을 곧바로 체불로 확정하거나 중복 합산하지 않는다.
   - doc-compare는 전월·당월 명세서와 같은 기간의 수정본 항목 차이에 쓴다. 비교는 변경 항목과 원문을 찾고, 확인된 값으로 별도 산술을 한다. 법률 판정은 하지 않는다. 모호한 비교는 보류한다.

4. 문의·상담
   - 회사 문의/상담을 선택한다.
   - 답변·보완 요청·수정 명세서를 연결한다.
   - 사용자 확인 후 쟁점별로 정리한다.
   - 지급 약속과 실제 지급을 따로 관리한다. 약속=입금, 차액 0=사건 자동종결로 처리하지 않는다.

5. 답변·추가 지급
   - 지급 약속과 실제 지급을 따로 관리한다.
   - 추가 지급이 있으면 실제 지급으로 반영하고, 남은 의문을 다시 정리한다.

6. 다음 달 기록
   - 이번 결과를 참고 상태로 남기고, 다음 급여기간 기록·비교를 이어갈 수 있게 한다.
   - 정정이 발생하면 관련 결과도 갱신한다.

7. 정정·이력·출력 갱신
   - 정정 전 원문·이력은 보존한다.
   - 관련 계산·질문·한국어와 선택 언어를 같은 사실·같은 버전으로 함께 확인·출력하는 결과까지 갱신한다.
   - 예전 출력은 구버전으로 표시한다.
   - 미기록 날짜를 실제 근무로 자동 확정하지 않는다.

8. 달력 관리
   - 달력에서 출퇴근·휴게·메모·증빙을 본다.
   - 상태는 예정/실제/회상/미기록으로 구분한다.
   - 정정하면 관련 결과를 갱신한다.
   - 지급 약속, 실제 지급, 남은 의문은 달력/흐름 안에서 따로 관리한다.

9. 언어·저장
   - 상단 언어는 한국어·영어·베트남어·네팔어·크메르어.
   - 한국어와 선택 언어를 같은 사실·같은 버전으로 함께 확인·출력하는 문의 문구·상담자료를 출력한다.
   - 내 기기 저장, 임시 이용, 백업/복원, 삭제를 제공한다.

## 6. 제외 범위

- 스토어 관련 기능
- 음성 입력/음성 인터페이스
- 은행 로그인
- 자동 연락/자동 신고
- 법률 판정(체불이냐 아니냐를 앱이 확정하지 않는다)
- 상담자 대리관리 기능
- 위 항목들은 이번 범위에서 제외한다.

## 7. 확인 가능한 완료 조건

### 7.1 기능 관점

- 4개 시작 버튼에서 각각 문답/수동 입력으로 시작할 수 있다.
- 자료가 없어도 확인 질문과 자료 요청으로 시작할 수 있고, 입력 내용은 저장되며 자료가 생기면 같은 급여기간 기록에서 이어간다.
- 버튼·언어를 바꿔도 같은 급여기간 기록의 입력 상태가 유지된다.
- 사진/PDF OCR은 원문 대조·사용자 확인을 거친다. 실패 시 수동으로 이어진다.
- 명세서와 해당 지급 대조, 전월·당월 비교, 입력 조건에 따른 기본급 산술을 구분한다.
- 지급은 해당 급여기간에 연결한 것만 다루고, 분할·현금·중복을 확인한다.
- 모르는 값을 0으로 채우지 않고, 차액을 체불로 확정하거나 중복 합산하지 않는다.
- doc-compare는 전월·당월 명세서와 같은 기간 수정본 항목 비교에 쓴다. 비교는 변경 항목과 원문을 찾고, 확인된 값으로 별도 산술을 한다. 법률 판정은 하지 않는다. 모호한 비교는 보류한다.
- 회사 문의/상담 선택 → 답변·보완 요청·수정 명세서 연결 → 사용자 확인 후 쟁점별 정리 흐름이 있다.
- 지급 약속과 실제 지급을 따로 관리하고, 약속=입금 또는 차액 0=사건 자동종결로 처리하지 않는다.
- 달력은 출퇴근·휴게·메모·증빙과 예정/실제/회상/미기록을 구분한다. 정정 시 관련 결과가 갱신된다.
- 정정 전 원문·이력을 보존하고, 관련 계산·질문·한국어와 선택 언어를 같은 사실·같은 버전으로 함께 확인·출력하는 결과까지 갱신하며, 예전 출력은 구버전으로 표시한다. 미기록 날짜를 실제 근무로 자동 확정하지 않는다.
- 지급 약속과 실제 지급, 남은 의문을 따로 관리한다.
- 상단 언어: 한국어·영어·베트남어·네팔어·크메르어.
- 한국어와 선택 언어를 같은 사실·같은 버전으로 함께 확인·출력하는 문의 문구·상담자료 출력, 내 기기 저장·임시 이용·백업/복원·삭제를 포함한다.

### 7.2 비기능·정책 관점

- 키·실제 원문(증빙 원본 등)이 공개되지 않도록 노출을 방지한다. 통과 기준: 앱 화면·출력 파일·로그에 키 원문 전체가 그대로 노출되지 않는다.
- API 실패 시 수동 진행과 재시도 상한을 둔다. 통과 기준: 외부 API 호출이 실패하면 입력 중이던 내용을 잃지 않고 수동 입력으로 이어가며, 동일 요청 재시도는 상한을 넘지 않는다.
- 복원 실패 시 기존 기록을 보존한다. 통과 기준: 백업 복원이 실패하면 기존 기록이 삭제·덮어쓰기되지 않는다.
- 모바일·데스크톱에서 자료 없음 → 저장 → 재방문이 이어진다. 통과 기준: 자료가 없는 상태에서도 입력 내용이 저장되고, 같은 급여기간 기록으로 다시 들어와 이어서 진행할 수 있다.
- 정정은 화면·양언어·출력 파일까지 이어지고 금액과 버전이 일치한다. 통과 기준: 한 언어에서 정정을 반영하면 같은 사실이 한국어와 선택 언어 출력, 화면 요약, 내보낸 파일에서도 같은 금액과 같은 버전 표기로 반영된다.
- 5언어 글자 깨짐·버튼 잘림이 없다. 통과 기준: 한국어·영어·베트남어·네팔어·크메르어 화면에서 글자가 깨지지 않고, 주요 버튼이 잘리거나 사라지지 않는다.
- 앱·PRD·시연은 같은 버전 기준으로 맞춘다. 통과 기준: 시연에서 보여주는 흐름과 PRD·앱에 적힌 범위·버전 표기가 같은 기준을 따른다.
- 개인정보 처리, API 비용, 오류 복구를 다룬다. 통과 기준: 저장할 개인정보 범위, 외부 API 사용 시 비용 발생 조건, 실패·중단 시 복구 절차가 문서에 적혀 있다.
- 법률 판정·자동 연락/신고·스토어·음성·은행 로그인·상담자 대리관리는 범위에서 제외한다.

### 7.3 미입증 상태

- 실제 사용자 검토: 아직 안 함.
- 번역 검토: 아직 안 함.

## 8. 가정·주의

- 앱은 확인·정리·준비를 돕는 도구지, 법률 판단이나 지급 명령을 내리는 주체가 아니다.
- 입력값은 항상 불완전할 수 있다. 미확인·추정·미기록은 따로 표시한다.
- doc-compare는 비교 보조 도구며, 변경 항목과 원문을 찾고 확인된 값으로 별도 산술을 한다. 법률 판정은 하지 않는다.
- 언어·문구 출력은 제공 대상이고, 실제 번역 품질은 별도 검토가 필요하다.
